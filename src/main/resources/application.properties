spring.application.name=user-service

# Update MySQL connection (Use 'database' instead of 'localhost')
spring.datasource.url=jdbc:mysql://database:3307/user_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Qwe123qatar#4

# Hibernate & JPA Settings
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update

# Update Eureka Server URL (Use 'eureka-server' instead of 'localhost')
eureka.client.service-url.default-zone=http://eureka-server:8762/eureka

# Server Port (Same as in Docker Compose)
server.port=8088

# Other Spring Configurations
spring.mvc.pathmatch.matching-strategy=ANT_PATH_MATCHER
spring.main.allow-circular-references=true
